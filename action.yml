name: 'my first container action'
description: 'Be all you can'
author: 'Arun'
inputs:
  myInput: 
    description: 'greeting to use'
    required: true
    default: 'Awesome guru'
    
 outputs:
  myOutput:
    description: 'total memory'
    
  runs:
    using: 'docker'
    image: 'Dockerfile'
